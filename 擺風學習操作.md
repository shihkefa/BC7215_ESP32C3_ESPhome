# v1.0.7：上下／左右擺風分開學習

完成原本的冷氣配對後，上下與左右可以各自學習，不需要重新匹配品牌。
只需要哪個方向，就學哪個方向。每個方向都需要一筆「擺風關」及一筆「擺風開」的完整遙控訊號。

這是原廠「替換基礎封包」功能的應用：學習擺風狀態，再讓碼庫產生目前溫度、模式、風速的控制碼。
並非自動查出碼庫中的擺風編號，也不是原封不動重播學習時的 25°C。

## v1.0.7 遙控器擺風同步

v1.0.6 只有學習／發送，收到遙控訊號時將擺風標為未知。本版增加已學習樣本的辨識：
在記憶體內把收到的完整封包與各方向的關／開樣本，分別交給碼庫產生冷房 25°C、自動風、
相同按鍵的封包，再比較有效資料及格式。這會消除正常設定及其校驗碼的差異，**不發射紅外線**。

- 唯一吻合關閉／上下／左右時，更新空調實體的擺風選項。
- 「擺風狀態」會顯示「依遙控樣本辨識」，仍不是冷氣主機回報。
- 完整比對不吻合、多方向歧義、開關差異被碼庫消除、收到關機或無法解析時，標示未知。
  ESPHome 擺風沒有未知選項，所以空調選單保留上次選擇，請搭配擺風狀態感測器判讀。
- 不以猜測的差異位元、模糊相似度或單次切換推定擺風。遙控器計數器、其他附加設定、
  不同按鍵格式等仍可能導致無法辨識；不保證所有品牌都能同步。
- 未學習的方向，以及兩軸同時開的狀態，無法據此可靠辨識。
- 比對結束恢復剛收到的完整封包，不把比對用的 25°C 套用到下一個控制指令。
- 沿用 v1.0.6 已保存的配對與擺風資料，通常不需要重新學習；SHT40、RGB、接線不變。

更新後先用原廠遙控器切換已學習方向的開／關，再改為 28°C 重試；確認 HA 擺風及溫度同時跟隨。
若未同步，查看「擺風狀態」及 DEBUG 日誌 `Swing RX`：state=-1 未知，0 關閉，1 上下，2 左右。
若沒有這行日誌，也請保留鄰近的 `IR RX`／解析失敗訊息，便於判斷是否收到適用的完整碼。

## 更新方式

用本包的 `bc7215a-c3.yaml` 與整個 `components/bc7215a` 更新原有專案。
請保留自己的 `secrets.yaml` 及 `logger_uart` 選擇。只換 YAML 不會得到新功能。
原本配對的資料結構與儲存鍵保留；擺風使用另外的儲存區。

本包是供編譯與確認的原始碼，沒有 BIN，也沒有刷入你的裝置。

## 以學習上下擺風為例

1. 確認原本開關、溫度、模式、風速控制正常，運作狀態顯示配對已保存。
2. 在 HA 按「學習上下擺風」。此時是準備階段，收到的遙控訊號不會存成樣本。
3. 遙控器維持冷氣開機、冷房 25°C、固定風速；關閉左右擺風。先把上下擺風設為開，讓下一次按鍵會變成關。
4. 等接收結束，在 HA 按「擷取擺風樣本」，再對準 BC7215A **按一下上下擺風，把它關閉**。
5. 等 HA 顯示「學習中：擺風開」。再次按「擷取擺風樣本」，再 **按一下上下擺風，把它開啟**。
6. 出現「擺風開關樣本已保存」即完成。從空調實體的擺風選單切換「上下」與「關閉」，觀察冷氣葉片。

學習左右時，把以上的上下／左右交換：**上下保持關閉**，分別擷取左右關、左右開。

關閉的是「擺風」，不是冷氣電源。兩筆樣本需使用同一支原廠遙控器，保持相同溫度、模式與風速；
25°C 冷房是操作建議，本程式也接受 16–30°C 內其他可解析的相同設定。
只按一下、放開並等待 HA 提示，不要連按或長按。若遙控器循環包含固定角度，需選到真正的擺風關／開狀態。

每個準備或擷取階段最多等待 90 秒；按「取消擺風學習」可離開。取消、逾時或不合格樣本不覆蓋已完成的舊學習。
學習期間 HA 空調控制暫停，板載彩燈以七彩提示，結束後熄滅。BC7215A 模組本身的燈不受此設定控制。

## 空調選單與狀態

空調實體固定提供下列三個選項，尚未學習的方向會拒絕發送，並在「運作狀態」提示。

| 選項 | 套用的樣本 | 結果的預期狀態 |
|---|---|---|
| 關閉 | 任一已完成方向的「關」樣本 | 上下關、左右關 |
| 上下 | 上下方向的「開」樣本 | 上下開、左右關 |
| 左右 | 左右方向的「開」樣本 | 上下關、左右開 |

**本版沒有「上下＋左右同時開」。** 兩組分開學到的完整封包無法通用地合成。
選擇另一方向時，會連同它的樣本套用另一軸關閉的設定。

- 「上下擺風學習狀態」與「左右擺風學習狀態」分開顯示。成功時是「已學習，待實機確認效果」。
- 程式只能確認收到可解析且不同的開／關資料，不能從資料差異證實葉片真的在動，也不能辨認你是否按對軸。
- 「擺風狀態」顯示依指令推定的狀態。碼庫本身沒有上下／左右解析欄位；v1.0.7 以已學習的完整樣本比對，唯一吻合時同步。
  無法辨識或重啟後顯示未知，空調選單保留上次選項；此時不能把它當成已同步的擺風狀態。
- 冷氣關機時修改擺風，先暫存選項，不會因此開機；下一次開機時套用。開機流程仍先送開機碼，再送目前設定。
- 同一通道的溫度／模式／風速修改會繼續使用所選樣本。收到原廠遙控器後，以其完整訊號作為基礎。正常修改溫度不會因為同步辨識而替換成學習時的樣本；
  再次明確選擇擺風或需要重建開機基礎封包時，才套用相應樣本。
- 學習資料綁定配對識別碼與碼庫版本所形成的指紋；不同候選或不同配對資料不會套用舊樣本。

## 相容範圍

- 需要同格式、可解析溫度／模式／風速及明確開機狀態的完整碼。只有獨立短切換碼、無法判斷開／關的切換命令，不適用此流程。
- 本版保守接受 1–3 段，段數與各段長度／簽章需和配對樣本相符。單段、各段合計及合併後儲存資料皆最多 255 bytes。
  包內實際碼庫在替換四段資料及大封包時有限制，因此拒絕這些學習樣本；這不表示冷氣本身沒有擺風功能。
- 學到的是完整封包的附加功能狀態。除了擺風，也可能帶有睡眠、定時、強力等設定；學習時請把這些附加功能關閉並保持一致。
- 若使用某個模式／風速後擺風不起作用，可能是該協議的按鍵格式或附加功能相互作用，需以該冷氣實測為準。
- 保存失敗會提示並保留記憶體中的舊樣本；快閃記憶體是否寫入成功不可視為已確認，需排除問題後重試。

## 建議驗收

先只學上下，將 HA 設為冷房 28°C、中風，再切換上下開／關，確認葉片與溫度／風速沒有回到學習值。
接著學左右，確認上下樣本仍可使用。最後重啟裝置，確認兩組學習狀態仍存在，且啟動沒有自動發射紅外線。

ESPHome 2026.8.2 已通過設定驗證與程式碼產生；主機模擬與原版碼庫測試見 `VALIDATION.md`。
尚未完成 C3 目標韌體編譯、實機擺風、斷電儲存或 HA 操作驗證。

依據：[原廠 BC7215 AC Control Library 手冊](https://github.com/bitcode-tech/bc7215_ac_lib/blob/master/docs/bc7215a_ac_lib_manual_en.pdf)，第 11 節 Replace Base Data Packet。
